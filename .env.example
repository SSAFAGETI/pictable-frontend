# API proxy base. Leave as /api when using Vite/Vercel rewrites.
VITE_API_BASE_URL=/api

# Google OAuth Web Client ID from Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=435711364471-928n4dkjm76ttqt9upkcnlhonlnsmc7c.apps.googleusercontent.com

# Optional. Must match the redirect URI registered in Google Cloud and Django token exchange settings.
# Local: http://localhost:3000/oauth/callback
# Vercel: https://ssafagetti.vercel.app/oauth/callback
VITE_GOOGLE_REDIRECT_URI=